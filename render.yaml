services:
  - type: web
    name: echemdb
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py --port=\ --host=0.0.0.0
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.12.0
    plan: free
